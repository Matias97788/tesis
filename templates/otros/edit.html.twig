{% extends 'base.html.twig' %}

{% block title %}Edit Otros{% endblock %}

{% block body %}
    {% include 'menu.html.twig' %}

<div class="container mt-5">
    <br>
     <div class="row ">
        <div class="col-md-12 text-center">
    <h1>Editar Servicios adicionales</h1>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4"></div>
        <div class="col-md-4 text-center"><a href="{{ path('otros_index') }}" class="btn btn-primary">Volver</a></div>
  </div>
    {{ include('otros/_form.html.twig', {'button_label': 'Actualizar'}) }}

</div>    
</div>
</div>
{% include 'footer.html.twig' %}
    
{% endblock %}