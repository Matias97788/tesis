{% extends 'base.html.twig' %}

{% block title %}Otros{% endblock %}

{% block body %}
{% include 'menu.html.twig' %}
    <div class="container mt-5">
    <br>
     <div class="row ">
        <div class="col-md-12 text-center">
    <h1>Servicios adicionales</h1>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4"></div>
        <div class="col-md-4 text-center"><a href="{{ path('otros_index') }}" class="btn btn-primary">Volver</a></div>
  </div>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ otro.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ otro.nombre }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ otro.correo }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ otro.telefono }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ otro.descripcion }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('otros/_delete_form.html.twig') }}

    </div>    
</div>
</div>
{% include 'footer.html.twig' %}
{% endblock %}
